<p style="color: green"><%= notice %></p>

<h1>Profiles</h1>

<div id="profiles">
  <% @profiles.each do |profile| %>
    <%= render profile %>
    <p>
      <%= link_to "Show this profile", profile %>
    </p>
<%# <%= cl_image_tag profile.photo.key, height: 300, width: 400, crop: :fill %> %>

  <% end %>
</div>

<%= link_to "New profile", new_profile_path if policy(Profile).new?%>
